# Aaron's dotfiles

Installation:

1. Add the following to `~/.bash_profile`:

```
# Run dotfiles
[ -n "$PS1" ] && source <DOTFILES_PATH>/.bash_profile;

```

2. Rub the following:

```
> ./terminal.sh
> ./brew.sh
```
